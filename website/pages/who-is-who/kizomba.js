import withLayout from '../../components/hoc/withLayout';
import PageTitle from '../../components/who-is-who/PageTitle';

const page = () => {
  return <div className="kizomba-djs">
    <PageTitle title="Kizomba DJ's" />
    <section>

    </section>
  </div>
}

export default withLayout(page)